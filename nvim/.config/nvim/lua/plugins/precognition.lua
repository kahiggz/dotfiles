return {
  -- {
  --   "tris203/precognition.nvim",
  --   event = "VeryLazy", -- Load when Vim is mostly ready
  --   config = function()
  --     require("precognition").setup({
  --       -- Customize options here
  --       highlight = "DiffAdd", -- Example highlight group
  --       delay = 100, -- Delay in milliseconds before previewing motion
  --       threshold = 2, -- Minimum character movement before triggering preview
  --     })
  --   end,
  -- },
}
